<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xianglin.com.retrofit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".rxjava.FirstActivity" />
        <activity android:name=".rxjava.MapActivity" />
        <activity android:name=".rxjava.ZipActivity" />
        <activity android:name=".rxjava.FlowableActivity" />
        <activity android:name=".rxjava.IntervalActivity" />

        <service
            android:name=".downloadservice.DownloadService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".downloadservice.DownloadActivity"></activity>
    </application>

</manifest>